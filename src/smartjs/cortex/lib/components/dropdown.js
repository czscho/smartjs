/** @file React component for dropdown functionality. */

export class Draggable extends React.Component {
    constructor ( props ) {
        super ( props );
        this.state = {};
    }

    render () {
        return React.createElement(
            "div"
        );
    }
};